import { useState } from 'react'
import { MealPlanGenerator } from './components/MealPlanGenerator'
import { MealPlanHistory } from './components/MealPlanHistory'
import { ToddlerTracker } from './components/ToddlerTracker'
import { Tabs, TabsContent, TabsList, TabsTrigger } from './components/ui/tabs'
import { Utensils, Baby, Sparkles, History } from 'lucide-react'
import { Toaster } from './components/ui/sonner'

function App() {
  return (
    <div className="min-h-screen relative">
      {/* Beautiful Header with gradient */}
      <header className="border-b border-[#16250F]/10 bg-gradient-to-b from-[#F5F1E8]/98 to-[#F5F1E8]/95 backdrop-blur-xl shadow-sm sticky top-0 z-50 animate-fade-in">
        <div className="max-w-7xl mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-3">
              <img 
                src="/pantrie_logo.svg" 
                alt="Pantrie" 
                className="h-10 w-auto" 
              />
              <p className="text-sm text-[#16250F]/70 font-serif">AI-powered family meal planning</p>
            </div>
          </div>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-4 sm:px-6 py-8 sm:py-12">
        <Tabs defaultValue="meal-plan" className="w-full">
          <TabsList className="grid w-full max-w-2xl grid-cols-3 mb-8 sm:mb-12 bg-[#F5F1E8] backdrop-blur-sm shadow-lg h-auto border border-[#16250F]/10 rounded-lg p-1.5">
            <TabsTrigger 
              value="meal-plan" 
              className="flex items-center gap-2 data-[state=active]:bg-[#16250F] data-[state=active]:text-[#F5F1E8] transition-all duration-200 rounded-md px-4 py-2.5 font-medium"
            >
              <Utensils className="h-4 w-4" />
              <span className="hidden sm:inline">Meal Planning</span>
              <span className="sm:hidden">Meals</span>
            </TabsTrigger>
            <TabsTrigger 
              value="history" 
              className="flex items-center gap-2 data-[state=active]:bg-[#16250F] data-[state=active]:text-[#F5F1E8] transition-all duration-200 rounded-md px-4 py-2.5 font-medium"
            >
              <History className="h-4 w-4" />
              History
            </TabsTrigger>
            <TabsTrigger 
              value="toddler" 
              className="flex items-center gap-2 data-[state=active]:bg-[#16250F] data-[state=active]:text-[#F5F1E8] transition-all duration-200 rounded-md px-4 py-2.5 font-medium"
            >
              <Baby className="h-4 w-4" />
              <span className="hidden sm:inline">Toddler Tracker</span>
              <span className="sm:hidden">Toddler</span>
            </TabsTrigger>
          </TabsList>

          <TabsContent value="meal-plan" className="mt-0">
            <MealPlanGenerator />
          </TabsContent>

          <TabsContent value="history" className="mt-0">
            <MealPlanHistory />
          </TabsContent>

          <TabsContent value="toddler" className="mt-0">
            <ToddlerTracker />
          </TabsContent>
        </Tabs>
      </main>

      <Toaster position="top-right" richColors />
    </div>
  )
}

export default App
